现象：侧边栏Dock需要悬浮展开；深度思考需要滑块控制档位。
根因：UI增强需求。
修复：修改 index.html, style.css 实现 dock-sidebar 悬停时的 tooltip 渐出动画；将 btn-deep-thinking 替换为 range slider，在 app.js 添加 deepThinkingLevel 逻辑，并修改 api.py 增加 0/1/2 档位逻辑。
教训：复杂的 UI 组件替换需要同时留意前端数据绑定和后端读取接口。
