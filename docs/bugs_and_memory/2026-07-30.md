现象 → ComfyUI 目录可配置但便携版解释器定位不稳定，绘图服务常重复启动；退出时还会强杀无关 Python 进程。语音、脚本箱、Zed、配额切号和第三方会话写入仅有模拟或不安全实现。
根因 → 路径解析、启动逻辑与绘图工作线程重复实现；缺少服务常量；模拟功能被暴露为可用功能。
修复 → 统一 ComfyUI 路径校验、解释器选择和启动流程，仅终止本应用持有的子进程；补齐服务常量；完全移除上述前后端功能与配置。
教训 → 不应将未对接真实 API 的模拟能力暴露为可执行功能；进程清理必须限定为本应用创建的进程。
